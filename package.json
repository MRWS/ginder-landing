{
  "name": "ginder-landing",
  "main": "index.html",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "deploy": "rm -rf ./build/* && parcel build index.html --out-dir build && cp -R CNAME ./build && cp -R particles.json ./build && surge ./build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MRWS/ginder-landing.git"
  },
  "devDependencies": {
    "autoprefixer": "^8.2.0",
    "parcel-bundler": "^1.7.0",
    "node-sass": "^4.8.3"
  },
  "dependencies": {
    "chart.js": "^2.7.2",
    "normalize.css": "^8.0.0",
    "particles.js": "^2.0.0"
  }
}
